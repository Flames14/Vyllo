<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default: Modern TLS only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Third-party lyrics APIs rely on rotating CDN certificates.
         Trust the system store here so lyric requests don't break when providers rotate certs. -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">lrclib.net</domain>
        <domain includeSubdomains="true">music.163.com</domain>
    </domain-config>

    <!-- Debug overrides (remove for production) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
